- name: variable
  hosts: WEB
  vars:
    URL: www.google.com
  tasks:
    - name: Print Variable
      ansible.builtin.debug:
        msg: URL = {{URL}}
# Ansible access  variable with two flower brackets.
