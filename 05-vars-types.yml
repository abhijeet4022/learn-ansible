- name: Variables Types
  hosts: localhost
  connection: local
  vars:
    fruit_name: Orange #String
    quantity: 100 # Number
    for_sale: true # Boolean, B
  tasks:
    - name: Print
      ansible.builtin.debug:
        msg: Fruit Name - {{ fruit_name}}, Quantity - {{quantity}}
      when: for_sale